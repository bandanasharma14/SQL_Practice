Select S.student_id,S.student_name,Sub.subject_name,Count(E.subject_name) As attended_exams From Students S join Subjects Sub left Join Examinations E on S.student_id=E.student_id and Sub.subject_name=E.subject_name group by S.student_name,Sub.subject_name order by student_id,subject_name
